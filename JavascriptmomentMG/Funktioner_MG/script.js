'use strict';
/*
function findBiggestFraction(a, b) {
    if (a > b) {
        console.log('a är störst');
    } else {
        console.log('b är störst');

    }
}

findBiggestFraction(1 / 5, 1 / 2);
*/

/*
function factorial(n) {

    var product = 1;
    for (var i = 1; i <= n; i++) {
        product = product * i;
    }

    return product;

}

console.log(factorial(4));

*/
/*
function range(n) {
    var array = new Array(n);

    return array;
}

console.log(range(5));
*/


/*



function random(max) {

    var number = Math.floor(Math.random() * max + 1);

    return number;
}



console.log(randomArray(10));

function randomArray(n) {

    var array = [];
    array.length = n;

    for (var i = 0; i < n; i++) {
        var result;
        result = random(100);
        array[i] = result;
    }

    return array;
}

console.log(randomArray(10));

*/
/*
var arr = function (n) {
    var array = new Array(n);

    return array;
}

console.log(arr(5));
*/
/*
var rand = function (max) {

    var number = Math.floor(Math.random() * max + 1);

    return number;
}

console.log(rand(5));
*/